<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="pricelist_item_list_view_action_server" model="ir.actions.server">
        <field name="name">See list of rules</field>
        <field name="model_id" ref="product.model_product_pricelist"/>
        <field name="binding_model_id" ref="product.model_product_pricelist"/>
        <field name="state">code</field>
        <field name="code">
            action = records.action_view_pricelist_rule_list()
        </field>
    </record>

</odoo>
